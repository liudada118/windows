# 门窗行业算料公式库说明文档

> 版本: V1.0 | 日期: 2026-03-02 | 数据来源: 画门窗(WindoorCraft)行业软件实际配置

## 1. 概述

本文档记录了从行业软件"画门窗"中提取的完整算料公式体系，并说明了如何将这些公式集成到 WindoorDesigner 系统中。

算料公式是门窗行业的核心知识，它决定了从设计图纸到实际生产所需的每一根型材的切割长度、每一块玻璃的尺寸、每一个五金件的数量。

## 2. 核心概念

### 2.1 变量体系

门窗算料公式使用一套标准化的变量体系，其中最核心的变量是 `cc`（Current Component，当前构件尺寸）：

| 变量名 | 含义 | 说明 |
|--------|------|------|
| `cc` | 当前构件计算尺寸 | 由上下文决定：框横向=窗宽，框纵向=窗高，扇横向=扇宽，扇纵向=扇高 |
| `frame_width` | 窗户总宽 | mm |
| `frame_height` | 窗户总高 | mm |
| `sash_width` | 扇宽 | mm |
| `sash_height` | 扇高 | mm |
| `sashCount` | 开启扇数量 | 整数 |
| `overlap` | 搭接数量 | 用于压线计算 |

### 2.2 连接类型 (connection)

中梃和压线的长度取决于两端连接的构件类型：

| 连接类型 | 含义 | 典型扣减 |
|----------|------|----------|
| `frame-frame` | 两端都连接外框 | 减两个外框小面 |
| `frame-mullion` | 一端连外框，一端连中梃 | 减一个外框小面 + 一个中梃半宽 |
| `mullion-mullion` | 两端都连接中梃 | 减两个中梃半宽 |

## 3. 108 平开系列公式（完整版）

### 3.1 型材公式

以下公式来源于画门窗软件 108 平开系列的实际配置，共 **34 条**型材公式。

#### 3.1.1 框/梃 (bar)

| 构件 | 型材名称 | 长度公式 | 数量 | 连接类型 | 说明 |
|------|----------|----------|------|----------|------|
| 外框 | 108外框 | `cc` | 1 | — | 上下横=窗宽，左右竖=窗高 |
| 中梃 | 108中梃 | `cc-40-40+5+5` | 1 | frame-frame | 减两个外框小面40，加端铣量5 |
| 中梃 | 108中梃 | `cc-40-20+5+5` | 1 | frame-mullion | 减外框小面40+中梃半宽20 |
| 中梃 | 108中梃 | `cc-20-20+5+5` | 1 | mullion-mullion | 减两个中梃半宽20 |
| 附框 | 108附框 | `cc-40-40` | 1 | frame-frame | 减两个外框小面 |
| 附框 | 108附框 | `cc-40-20` | 1 | frame-mullion | |
| 附框 | 108附框 | `cc-20-20` | 1 | mullion-mullion | |
| 压线 | 108压线 | `cc-40-40-overlap*22` | 1 | frame-frame | overlap=搭接数, 22=压线宽 |
| 压线 | 108压线 | `cc-40-20-overlap*22` | 1 | frame-mullion | |
| 压线 | 108压线 | `cc-20-20-overlap*22` | 1 | mullion-mullion | |
| 转角 | 108转角 | `cc` | 1 | — | 转角连接件 |

#### 3.1.2 开启扇 (sash)

| 构件 | 型材名称 | 长度公式 | 数量 | 方向 | 说明 |
|------|----------|----------|------|------|------|
| 扇 | 108扇 | `cc` | 1 | — | 上下横=扇宽，左右竖=扇高 |
| 纱扇 | 108纱扇 | `cc` | 1 | — | 同扇 |
| 防盗框 | 108防盗框 | `cc` | 1 | — | 同扇 |
| 防盗杆 | 108防盗杆 | `cc-20-20` | 1 | frame-frame | 减两端连接件 |
| 扇压线 | 108压线 | `cc-54-54-22-22` | 1 | 上/下 | 横压线：减扇小面54，减压线宽22 |
| 扇压线 | 108压线 | `cc-54-54` | 1 | 左/右 | 竖压线：减扇小面54 |

#### 3.1.3 对开扇 (doubleSash)

| 构件 | 型材名称 | 长度公式 | 数量 | 说明 |
|------|----------|----------|------|------|
| 扇 | 108扇 | `(cc-10)/2` | 2 | 总宽减中间缝隙10再除2 |
| 假中梃 | 108假中梃 | `cc` | 1 | 安装在右扇上 |

### 3.2 玻璃公式

| 类型 | 宽度公式 | 高度公式 | 连接类型 | 说明 |
|------|----------|----------|----------|------|
| 固玻 | `cc-40-40-6` | `cc-40-40-6` | frame-frame | 减外框小面40，减垫块缝隙6 |
| 固玻 | `cc-40-20-6` | `cc-40-20-6` | frame-mullion | |
| 固玻 | `cc-20-20-6` | `cc-20-20-6` | mullion-mullion | |
| 扇玻 | `cc-70-6` | `cc-70-6` | — | 减扇料小面70，减垫块缝隙6 |

### 3.3 辅材公式

| 名称 | 数量公式 | 适用范围 | 说明 |
|------|----------|----------|------|
| 螺丝3.0X19mm | `(frame_width*2+frame_height*2)/500` | bar | 每500mm一颗 |
| 玻璃胶 | `(((frame_width*4+frame_height*4)/10)*.7*.7)/300` | bar | 按周长计算 |
| 外框胶条3mm | `glass_perimeter/1000` | bar | 按玻璃周长计算(米) |
| 压线胶条4mm | `glass_perimeter/1000` | bar | 按玻璃周长计算(米) |
| 扇胶条 | `(sash_perimeter/1000)*2` | sash | 内外各一圈 |
| 螺丝3.9X19mm | `sash_perimeter/500` | sash | 每500mm一颗 |
| 扇执手 | `1` | sash | 每扇一个 |

### 3.4 报价公式

| 计价项 | 单价 | 计价方式 | 说明 |
|--------|------|----------|------|
| 平方 | ¥1250/m² | 按面积 | 最小计价面积2.0m² |
| 开启 | ¥1000/个 | 按扇数 | 每个开启扇加收 |
| 大玻璃 | 材料价 | 按块 | 面积>2.5m²的玻璃按实际材料价 |
| 转角 | ¥300/个 | 按个 | 每个转角加收 |

## 4. 推拉窗公式（基础版）

推拉窗与平开窗的主要区别在于：
- 框分为**上滑**和**下滑**（而非统一的外框）
- 扇分为**光企**和**勾企**（而非统一的扇料）
- 扇之间有**交错重叠**（interlock）

详细公式见代码文件 `packages/shared/src/formulas/series/sliding-90.ts`。

## 5. 代码集成说明

### 5.1 文件结构

```
packages/shared/src/
├── formulas/
│   ├── index.ts              # 公式库入口
│   ├── engine.ts             # 算料引擎核心计算模块
│   └── series/
│       ├── casement-108.ts   # 108平开系列公式
│       └── sliding-90.ts     # 90推拉系列公式
├── types/
│   └── formula.ts            # 公式类型定义
└── constants/
    └── profiles.ts           # 型材系列 → 公式映射
```

### 5.2 使用方式

```typescript
import { formulaLibrary, calculateMaterials, extractWindowInput } from '@windoor/shared';

// 1. 获取公式
const formula = formulaLibrary['casement-108'];

// 2. 从窗户数据提取输入
const input = extractWindowInput(windowUnit);

// 3. 计算材料清单
const result = calculateMaterials(formula, input);

// result.profiles  → 型材清单
// result.glass     → 玻璃清单
// result.addons    → 辅材清单
// result.quote     → 报价明细
// result.totalPrice → 总价
```

### 5.3 扩展新系列

添加新的型材系列公式只需：

1. 在 `formulas/series/` 下新建文件
2. 按 `Formula` 接口定义公式
3. 在 `formulas/index.ts` 中注册
4. 在 `constants/profiles.ts` 的 `SERIES_FORMULA_MAP` 中映射

## 6. 数据来源声明

本公式库的数据来源于画门窗(WindoorCraft)行业软件的 108 平开系列实际配置，经过交叉验证（与订单 FG2026030101 的实际算料结果对比）确认准确性。
